# Secret Kubernetes pour les credentials de la base de données PostgreSQL.
# Les valeurs DOIVENT être encodées en base64 (echo -n "valeur" | base64)
apiVersion: v1
kind: Secret
metadata:
  name: vitalsync-db-secret
  namespace: default
type: Opaque
data:
  # echo -n "vitalsync_user" | base64
  POSTGRES_USER: dml0YWxzeW5jX3VzZXI=
  # echo -n "vitalsync_secret_2026" | base64
  POSTGRES_PASSWORD: dml0YWxzeW5jX3NlY3JldF8yMDI2
  # echo -n "vitalsync_db" | base64
  POSTGRES_DB: dml0YWxzeW5jX2Ri
